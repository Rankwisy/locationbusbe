---
import { nav } from '../data/site';

const currentPath = Astro.url.pathname;
---

<header role="banner">
  <div class="container">
    <nav aria-label="Navigation principale">
      <a href="/" aria-current={currentPath === '/' ? 'page' : undefined} class="nav-logo">
        <img src="/logo.png" alt="LocationBus - Location de bus avec chauffeur à Bruxelles" width="140" height="48" />
      </a>
      {nav.map((item) => (
        <a
          href={item.href}
          aria-current={currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)) ? 'page' : undefined}
        >
          {item.label}
        </a>
      ))}
      <a href="tel:+3224460191">+32 2 446 01 91</a>
    </nav>
  </div>
</header>
